// src/app/app.routes.ts (Estrutura Correta)

import { Routes } from '@angular/router';

export const routes: Routes = [
    // Você não precisa de nenhuma rota adicional aqui, 
    // pois a lógica de navegação (aba: 'login' | 'cadastrar' | 'todas' etc.) 
    // está sendo feita internamente no TarefasComponent, sem usar rotas do Angular.
];